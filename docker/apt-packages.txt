# Runtime OS packages for the final image.
# TeX Live itself is installed from CTAN in the builder stage, not from apt.

ca-certificates
curl
fontconfig
perl

# Required by the `svg` package to rasterise assets/*.svg at build time.
inkscape
